import { NextResponse } from "next/server";
export function middleware(req){if(!req.nextUrl.pathname.startsWith("/api/"))return NextResponse.next();const origin=req.headers.get("origin");const allowed=new Set(["https://noadua.com","https://www.noadua.com","http://localhost:3000"]);const h=new Headers();if(origin&&allowed.has(origin)){h.set("Access-Control-Allow-Origin",origin);h.set("Access-Control-Allow-Credentials","true");h.set("Access-Control-Allow-Methods","GET,POST,PUT,PATCH,DELETE,OPTIONS,HEAD");h.set("Access-Control-Allow-Headers","Content-Type, Authorization");h.set("Vary","Origin")}if(req.method==="OPTIONS")return new NextResponse(null,{status:204,headers:h});const res=NextResponse.next();h.forEach((v,k)=>res.headers.set(k,v));return res;}
export const config={matcher:["/api/:path*"]};
